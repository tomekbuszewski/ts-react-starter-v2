import {{camelCase name}} from "./{{camelCase name}}";

describe("Utils / {{camelCase name}}", () => {
    it("should return true", () => {
        const result = {{camelCase name}}();
        expect(result).toBeTruthy();
    });
});
